// navbar.component.ts
import { Component } from '@angular/core';

@Component({
  selector: 'app-navbar',
  standalone: true,
  imports: [], // Add other necessary imports if needed
  templateUrl: './navbar.component.html',    // Ensure this points to the correct path
  styleUrls: ['./navbar.component.css']      // Corrected `styleUrls`
})
export class NavbarComponent {
  // Navbar component logic here
}
