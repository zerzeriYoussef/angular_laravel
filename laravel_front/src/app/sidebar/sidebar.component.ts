// sidebar.component.ts
import { Component } from '@angular/core';

@Component({
  selector: 'app-sidebar',
  standalone: true,
  imports: [], // Add other necessary imports if needed
  templateUrl: './sidebar.component.html',   // Ensure this points to the correct path
  styleUrls: ['./sidebar.component.css']    // Corrected `styleUrls`
})
export class SidebarComponent {
  // Sidebar component logic here
}
